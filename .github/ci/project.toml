# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "tailrocks/tailrocks-gradle-conventions"
profile = "generic"
verified = true
default_branch = "main"
runners = "both"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["renovate-configuration"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files."]

[workflow]
github_runner = "ubuntu-24.04"
velnor_labels = ["self-hosted", "velnor-target-mvp"]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-gradle.yml", "maintenance.yml", "nightly.yml"]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "gradle"
label = "Gradle"
kind = "gradle"
root = "."
watch = ["build.gradle", "build.gradle.kts", "gradle/**", "gradlew", "gradlew.bat", "settings.gradle", "settings.gradle.kts"]
github_pr_commands = ["./gradlew check --no-daemon"]
github_full_commands = ["./gradlew check --no-daemon"]
velnor_pr_commands = ["./gradlew check --no-daemon"]
velnor_full_commands = ["./gradlew check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-buildsrc"
label = "Gradle (buildSrc)"
kind = "gradle"
root = "buildSrc"
watch = ["buildSrc/build.gradle", "buildSrc/build.gradle.kts", "buildSrc/gradle/**", "buildSrc/gradlew", "buildSrc/gradlew.bat", "buildSrc/settings.gradle", "buildSrc/settings.gradle.kts"]
github_pr_commands = ["cd -- 'buildSrc' && gradle check --no-daemon"]
github_full_commands = ["cd -- 'buildSrc' && gradle check --no-daemon"]
velnor_pr_commands = ["cd -- 'buildSrc' && gradle check --no-daemon"]
velnor_full_commands = ["cd -- 'buildSrc' && gradle check --no-daemon"]
[unit.cache]
key_files = ["buildSrc/gradle/wrapper/gradle-wrapper.properties", "buildSrc/gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-idea-conventions"
label = "Gradle (idea-conventions)"
kind = "gradle"
root = "idea-conventions"
watch = ["idea-conventions/**/*.gradle", "idea-conventions/**/*.gradle.kts", "idea-conventions/src/**"]
github_pr_commands = ["./gradlew :idea-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :idea-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :idea-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :idea-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-java-conventions"
label = "Gradle (java-conventions)"
kind = "gradle"
root = "java-conventions"
watch = ["java-conventions/**/*.gradle", "java-conventions/**/*.gradle.kts", "java-conventions/src/**"]
github_pr_commands = ["./gradlew :java-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :java-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :java-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :java-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-junit-conventions"
label = "Gradle (junit-conventions)"
kind = "gradle"
root = "junit-conventions"
watch = ["junit-conventions/**/*.gradle", "junit-conventions/**/*.gradle.kts", "junit-conventions/src/**"]
github_pr_commands = ["./gradlew :junit-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :junit-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :junit-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :junit-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-kotlin-conventions"
label = "Gradle (kotlin-conventions)"
kind = "gradle"
root = "kotlin-conventions"
watch = ["kotlin-conventions/**/*.gradle", "kotlin-conventions/**/*.gradle.kts", "kotlin-conventions/src/**"]
github_pr_commands = ["./gradlew :kotlin-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :kotlin-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :kotlin-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :kotlin-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-maven-publish-conventions"
label = "Gradle (maven-publish-conventions)"
kind = "gradle"
root = "maven-publish-conventions"
watch = ["maven-publish-conventions/**/*.gradle", "maven-publish-conventions/**/*.gradle.kts", "maven-publish-conventions/src/**"]
github_pr_commands = ["./gradlew :maven-publish-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :maven-publish-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :maven-publish-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :maven-publish-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-signing-conventions"
label = "Gradle (signing-conventions)"
kind = "gradle"
root = "signing-conventions"
watch = ["signing-conventions/**/*.gradle", "signing-conventions/**/*.gradle.kts", "signing-conventions/src/**"]
github_pr_commands = ["./gradlew :signing-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :signing-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :signing-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :signing-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-sonarqube-conventions"
label = "Gradle (sonarqube-conventions)"
kind = "gradle"
root = "sonarqube-conventions"
watch = ["sonarqube-conventions/**/*.gradle", "sonarqube-conventions/**/*.gradle.kts", "sonarqube-conventions/src/**"]
github_pr_commands = ["./gradlew :sonarqube-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :sonarqube-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :sonarqube-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :sonarqube-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-spotless-conventions"
label = "Gradle (spotless-conventions)"
kind = "gradle"
root = "spotless-conventions"
watch = ["spotless-conventions/**/*.gradle", "spotless-conventions/**/*.gradle.kts", "spotless-conventions/src/**"]
github_pr_commands = ["./gradlew :spotless-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :spotless-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :spotless-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :spotless-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]

[[unit]]
id = "gradle-versions-conventions"
label = "Gradle (versions-conventions)"
kind = "gradle"
root = "versions-conventions"
watch = ["versions-conventions/**/*.gradle", "versions-conventions/**/*.gradle.kts", "versions-conventions/src/**"]
github_pr_commands = ["./gradlew :versions-conventions:check --no-daemon"]
github_full_commands = ["./gradlew :versions-conventions:check --no-daemon"]
velnor_pr_commands = ["./gradlew :versions-conventions:check --no-daemon"]
velnor_full_commands = ["./gradlew :versions-conventions:check --no-daemon"]
[unit.cache]
key_files = ["gradle/wrapper/gradle-wrapper.properties", "gradle/libs.versions.toml"]
paths = ["~/.gradle/caches", "~/.gradle/wrapper"]
